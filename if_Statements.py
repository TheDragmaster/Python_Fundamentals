n = 1
if n > 2:
    n -= 1
elif n == 2:
    n *= 2
else:
    n += 2

    # and = &&
    # or = ||
    #Parentheses are only used in multi line conditions
x, y = 1, 2
if ((x > 2 and
     y != y) or n == y):
    x += y 
